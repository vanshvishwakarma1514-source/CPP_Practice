#include<iostream>
#include<string>

// in cpp string is  a  special class that is created to make string handeling easy
// the memory where the charachter of string is saved  is a char* buffer which is dynamically allocated 
//to optimize the memory the size of the buffer is expanded on the basis of its need
// on cpp when initialze contain 15 memory location at the begining.
//if a bigger string is save into this buffer the memory  is realocated to the bigger size.
